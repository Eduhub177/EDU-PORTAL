kind = "web"
previewPath = "/"
title = "EDU PORTAL"
version = "1.0.0"
id = "artifacts/edu-portal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21120

[services.development]
run = "pnpm --filter @workspace/edu-portal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/edu-portal", "run", "build" ]
publicDir = "artifacts/edu-portal/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21120"
BASE_PATH = "/"
